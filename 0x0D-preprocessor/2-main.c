#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: incoming parameter, unused
 * @argv: incoming parameter
 *
 * Return: Always 0 (Success)
 */


int main(int argc __attribute__((unused)), char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}
